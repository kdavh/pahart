server {
    listen 80;
    server_name pahart.com *.pahart.com;

    location / {
        # server_name localhost;
        proxy_pass http://web:3000;
        proxy_set_header  X-Real-IP  $remote_addr;
    }
}
